<Window x:Class="ManufacturingERP.Desktop.Views.WarehouseDialogWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="Warehouse" Height="360" Width="430" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <Grid>
            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
            <TextBlock Text="Name"/><TextBox Margin="0,20,0,8" Text="{Binding Name}" />
            <TextBlock Grid.Row="1" Text="Location"/><TextBox Grid.Row="1" Margin="0,20,0,8" Text="{Binding Location}" />
            <CheckBox Grid.Row="2" Content="Active" IsChecked="{Binding IsActive}" Margin="0,16,0,0"/>
        </Grid>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Width="100" Margin="0,12,8,0" Click="Save_Click"/>
            <Button Content="Cancel" Width="100" Margin="0,12,0,0" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
