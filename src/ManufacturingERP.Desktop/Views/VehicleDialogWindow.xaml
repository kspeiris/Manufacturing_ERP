<Window x:Class="ManufacturingERP.Desktop.Views.VehicleDialogWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="Vehicle" Height="460" Width="460" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <Grid>
            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
            <TextBlock Text="Vehicle Number"/><TextBox Margin="0,20,0,8" Text="{Binding VehicleNumber}" />
            <TextBlock Grid.Row="1" Text="Description"/><TextBox Grid.Row="1" Margin="0,20,0,8" Text="{Binding Description}" />
            <TextBlock Grid.Row="2" Text="Driver"/><TextBox Grid.Row="2" Margin="0,20,0,8" Text="{Binding DriverName}" />
            <TextBlock Grid.Row="3" Text="Sales Rep"/><TextBox Grid.Row="3" Margin="0,20,0,8" Text="{Binding SalesRepName}" />
            <CheckBox Grid.Row="4" Content="Active" IsChecked="{Binding IsActive}" Margin="0,16,0,0"/>
        </Grid>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Width="100" Margin="0,12,8,0" Click="Save_Click"/>
            <Button Content="Cancel" Width="100" Margin="0,12,0,0" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
