<Window x:Class="ManufacturingERP.Desktop.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Manufacturing ERP - Login" Height="520" Width="900" MinHeight="520" MinWidth="900"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" Background="{StaticResource AppBackgroundBrush}">
    <Grid Background="{StaticResource AppBackgroundBrush}" Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.2*" />
            <ColumnDefinition Width="0.9*" />
        </Grid.ColumnDefinitions>

        <Border Background="{StaticResource HeroBrush}" CornerRadius="28,0,0,28" Padding="42">
            <StackPanel VerticalAlignment="Center">
                <Border Background="#1FFFFFFF" CornerRadius="14" Padding="10,6" HorizontalAlignment="Left">
                    <TextBlock Text="ERP Workspace" Foreground="White" FontWeight="SemiBold" />
                </Border>
                <TextBlock Text="Manufacturing ERP" FontSize="36" FontWeight="Bold" Foreground="White" Margin="0,18,0,0" />
                <TextBlock Text="Factory, warehouse, van sales, collections and reporting in one desktop system." 
                           Margin="0,14,0,0" Foreground="#DDE4FF" TextWrapping="Wrap" FontSize="16" />
                <Border Background="#1FFFFFFF" CornerRadius="20" Padding="18" Margin="0,26,0,0">
                    <StackPanel>
                        <TextBlock Text="What you can do here" Foreground="White" FontWeight="SemiBold" />
                        <TextBlock Text="Track inventory, process sales, manage procurement, record production, and review finance in one place."
                                   Margin="0,8,0,0"
                                   Foreground="#E0EBFF"
                                   TextWrapping="Wrap" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>

        <Border Grid.Column="1"
                Padding="34"
                Background="#F5FFFFFF"
                BorderBrush="{StaticResource BorderBrushSoft}"
                BorderThickness="1"
                CornerRadius="0,28,28,0">
            <StackPanel>
                <TextBlock Text="Sign in" FontSize="28" FontWeight="SemiBold" Margin="0,12,0,8" />
                <TextBlock Text="Enter your credentials to open the ERP workspace." Style="{StaticResource PageSubtitleText}" Margin="0,0,0,24" />
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="Username" Margin="0,0,0,14" />
                <PasswordBox x:Name="PasswordBox" PasswordChanged="PasswordBox_OnPasswordChanged" Margin="0,0,0,14" materialDesign:HintAssist.Hint="Password" />
                <TextBlock Text="{Binding ErrorMessage}" Foreground="Crimson" Margin="0,0,0,14" TextWrapping="Wrap" />
                <Button Content="Login" Click="LoginButton_OnClick" Style="{StaticResource PrimaryActionButton}" Height="44" />
                <TextBlock Text="Secure desktop access for operations, stock, production, and finance."
                           Margin="0,16,0,0"
                           Foreground="{StaticResource InkMutedBrush}"
                           TextWrapping="Wrap" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
